apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  creationTimestamp: null
  labels:
    move2kube.konveyor.io/service: "1"
  name: "1"
spec:
  rules:
    - host: 1.com
      http:
        paths:
          - backend:
              service:
                name: el-1-git-repo
                port:
                  name: port-8080
            path: /el-1-git-repo
            pathType: Prefix
status:
  loadBalancer: {}
